 #           WEEK 1 
 ## 1,
  **source code** : mã nguồn là những gì bạn viết ra 
  **machine code** : mã máy là những chuỗi số nhị phân ( o và 1 ) cực dài và phức tạp ( ngôn ngữ của máy tính ) 
  **compiler** : trình biên dịch 
  source code --> compiler--> machine code 
**visual studio code ( vs code )**
## terminal or command-line interface ( CLI ) 
--> giao diện dòng lệnh là trong đó bạn nhập các lệnh 
hay **terminal window** : cửu sổ dòng lệnh đây sẽ là nơi bạn chạy các lệnh để biên dịch mã và ghi dữ liệu trong mã lập trình
## các lệnh Terminal đã học :
* `code hello.c`: dùng để tạo hoặc mở file code
* `make hello` : là người quản lý . nó ko **biên dịch** mà ra lệnh trình biên dịch dịch file này
* `./hello` : chạy chương trình đó ngay tại đây
```c
#include <stdio.h>

int main (void)
{
printf ("hello,word\n");
}
```
**graphical user interface**: giao diện người dùng độ hoạ (GUI)
* **library** là các mã máy do người khác viết đã đc biên dịch mà bạn có thể sử dụng
vd **manual.cs50.io**
* **header files** là mục lục . bạn đọc nó để biết thư viện có gì và dùng thế nào ( hay là máy tính quét nó để biết xem trong đó có gì )
vd **stdio.h** ,**cs50.h**
```c
#include <cs50.h>
#include <stdio.h>

int main (void)
{
string answer = get_string ("what's your name ?"); 
printf ("hello,%s\n",answer); = hàm printf là hiển thị văn bản , \n là xuống dòng mới , 
}
```
**`terminal`**
* `make hello`
* `./hello`
* `what's your name ? Loc`
* `hello, loc`
---
  1. " string" nghĩa là văn bản
* `;` là dùng kết thúc câu 
* `get_string` LÀ được dùng để lấy chuỗi từ ng dùng
* `answwer` là một nơi lưu trữ đặc biệt mà chúng ta gọi là biến . answer nó kiểu dữ liệu string và có thể chứa bất kỹ chuỗi ký tự nào bên trong nó
*  `%s` là một ký tự giữ chuỗi được gọi là mã định dạng, báo cho printf hàm biết cần chuẩn bị để nhận một giá trị string. `answwer` là giá trị string được truyền cho hàm `%s`
## 2, LINUX
`rm` + tên file ---> xoá file 
`mkdir` + tên file ---> tạo thư mục ( nếu trong file đó có dữ liệu không thì chức năng đó sẽ xoá thư mục )
`mv` ---> di chuyển và đổi tên
có 2 trường hợp 
1. `mv hello.c hello`
* trường hợp a : nếu **hello** là thư mục thì linnux sẽ di chuyển file **hello.c** vào bên trong thư mục **hello**
* trường hợp b : nếu **hello** không tồn tại thì linux sẽ đổi tên file từ **hello.c** sang h**hello**
* trường hợp c : nếu **hello** là file khác thì linux sẽ ghi đè nội dung của hello.c lên file hello đó
2. `mv hello.c hello/`
  * khảng định bảo linux chuyển file **hello.c** vào thư mục **hello**
 3. `mv+ tên file ..` là lên cấp bậc cao hơn
    vd mv hello.c..
  
---
`ls` ( list) là một lệnh dùng để liệt kê danh sách các mục trong một thư mục
`cd` ( change directory ) là một lệnh dùng để thay đổi thư mục lm vc hiện hành của hệ điều hành
`cd..` rời khỏi thư mục này 
`cp` : sao chép file này sang file khác
## 3, conditional ( câu điều kiện )
* nếu... thì...
```c
if ( x < y )
{
    printf ("x is less than y\n");        ( thụt lề 4 dòng )
}
```
* nếu ... thì ...không thì
* ```c
  if ( x < y )
  {
      printf ("x is less than y\n");
  }
  else
  {
      printf ("x is not less than y\n");
  }
  ```
  ! dấu bằng được biểu thị bằng ( == )
  vd if ( x == y ) 
  ## 4, types
  variables ( biến )
  * trong scracth
  (tạo biến set counter to 0) --->
  ```c
  int counter = 0;
  ```
  **phép gán** là lấy giá trị bên phải sao chép sang bên trái
  * trong scracth
  ( change counter by 1 )---->
```c
counter = counter + 1;
```
hoặc 
```c
counter += 1;
```
hoặc 
```c
counter++;
  
